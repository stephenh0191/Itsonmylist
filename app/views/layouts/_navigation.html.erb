<div class="container">
        <% if current_user.present? %>
        <div class="dropdown">
            <li class=current_user>
                <button class="dropbtn">Welcome</button>
                <div class="dropdown-content">
                    <ul>
                        <li class='login'><%= link_to "Edit Account",  edit_user_registration_path %></li>
                        <li class='login'><%= link_to "About", about_path %></li>
                        <li class='login'><%= link_to "contact", contact_path %></li>
                        <li class="dropdown-divider"></li>
                        <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
                </div>
            </li>
        </div>
        <% else %>
        <li><%= link_to "Log In", new_user_session_path , class: "btn btn-danger"%></li>
        <% end %>
        </ul>
    </div>
    <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">
                 <%= link_to "It's on my List", root_path, class:"navbar-brand"  %>
            </div>
    </nav>
    